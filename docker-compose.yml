version: '3.8'
services:
  backend:
    build:
      context: ./backend
    ports:
      - '5000:5000'
    environment:
      PORT: 5000
      JWT_SECRET: smartbuy-ai-super-secret
      FRONTEND_URL: http://localhost:5173
  frontend:
    build:
      context: ./frontend
    ports:
      - '5173:80'
